## Commit convention

This project follows Conventional Commits (adopted from Sep 2026).

Format: `type(scope): imperative description`

Types: feat, fix, refactor, test, chore, docs, ci

## Workflow

- No direct pushes to `main`
- One branch per change, merged via pull request
- CI must pass before merge
